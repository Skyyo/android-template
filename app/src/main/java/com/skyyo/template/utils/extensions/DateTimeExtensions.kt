package com.skyyo.template.utils.extensions

// date & time
// fun localDateTime() = LocalDateTime.now().withNano(0).toString()
//
// fun dateTimeWithOffset() = OffsetDateTime.now().withNano(0).toString()
//
// fun String.toLocalDateTime() = LocalDateTime.parse(this).toString()
//
// fun String.toDateTimeWithOffset() = OffsetDateTime.parse(this).withNano(0).toString()

// date
// fun localDate() = LocalDate.now().toString()
//
// fun dateWithOffset() = OffsetDateTime.now().format(DateTimeFormatter.ISO_OFFSET_DATE).toString()
//
// fun String.toLocalDate() =
//    LocalDateTime.parse(this).format(DateTimeFormatter.ISO_LOCAL_DATE).toString()
//
// fun String.toOffsetDate() =
//    OffsetDateTime.parse(this).format(DateTimeFormatter.ISO_OFFSET_DATE).toString()

// time
// fun localTime() = LocalTime.now().withNano(0).toString()
//
// fun timeWithOffset() = OffsetTime.now().withNano(0).toString()
//
// fun String.toLocalTime() = LocalDateTime.parse(this).toLocalTime().toString().toString()
//
// fun String.toOffsetTime() = OffsetDateTime.parse(this).toOffsetTime().toString().toString()
